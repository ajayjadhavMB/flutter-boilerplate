# Project identification
sonar.projectKey=flutter-boilerplate
sonar.projectName=flutter-boilerplate
sonar.host.url=https://unredeemable-conjunctly-sunni.ngrok-free.dev
sonar.login=sqa_bfdb95ec8ab74ee2dd97576bf058d7cb5f397d1b

sonar.projectVersion=1.0
sonar.sources=lib
sonar.tests=test
# Ignore generated files
sonar.exclusions=**/*.g.dart,**/*.freezed.dart,**/*.pb.dart

# Dart/Flutter coverage reports
sonar.javascript.lcov.reportPaths=coverage/lcov.info

sonar.dart.analysis.useDartAnalyzer=true
sonar.flutter.analyze=true

# Connection to SonarQube server and token
# sonar.host.url=https://sonar.mindbowser.com
## NOTE: the scanner reads configuration from this file, so you can run
## `sonar-scanner` without CLI parameters. For authentication prefer using
## an environment variable. This avoids committing secrets to the repo.
## Set the environment variable SONAR_TOKEN before running, for example:
##   export SONAR_TOKEN=your_token_here
## then simply run:
##   sonar-scanner
## The property below tells the scanner to use the SONAR_TOKEN environment
## variable if present.
## Use the SONAR_TOKEN environment variable if present. Note the correct
## substitution syntax is ${env.SONAR_TOKEN} (no colon). The scanner will
## accept either sonar.token (preferred) or sonar.login (deprecated).
## IMPORTANT: you've asked to embed the token directly in this file. This is
## insecure because the token will be stored in plain text and may be
## committed to source control; prefer using environment variables for CI.
## The literal token below will be used by sonar-scanner when you run it
## without CLI parameters or environment variables.
# sonar.token=sqa_e66d54e3fc0c2d551fbdc933b4f2999042b84ee3

## NOTE: sonar.login is deprecated in favor of sonar.token and is not set
## here to avoid the "both sonar.login and sonar.token are set" warning.

# Source and test configuration
## Limit sources to the Dart/Flutter source tree so test files are not
## accidentally included as both sources and tests (which causes indexing
## conflicts like "File ... can't be indexed twice").
# sonar.sources=lib
# test files directory (scanner will know these are tests)
# sonar.tests=test
# sonar.sourceEncoding=UTF-8

# Analyzer mode (DETECT is default)
# Options: DETECT, FLUTTER, DART, MANUAL, DARTANALYZER
# Defaults to DETECT
# sonar.dart.analyzer.mode=DETECT

# To override existing analyzer options
# sonar.dart.analyzer.options.override=true

# For manual analyzer mode, specify paths
# sonar.dart.analyzer.report.mode=
# sonar.dart.analyzer.report.path=

# Optional: exclude generated files
# sonar.exclusions=test/**/*_test.mocks.dart,lib/**/*.g.dart
